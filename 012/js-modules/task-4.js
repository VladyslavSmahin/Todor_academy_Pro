export function task_4() {
    function generateUniqueId() {
    let id = '';
        function getRandomId() {
        let newId = Math.random();
        return id = newId;
        }
        getRandomId();
        console.log(id);
        getRandomId();
        console.log(id);
        getRandomId();
        console.log(id);
    }
    generateUniqueId();
}